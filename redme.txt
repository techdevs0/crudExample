# Steps to Follow 
    clone the project from https://github.com/techdevs0/crudExample
    in the main folder run 
        => docker-compose build --no-cache
        => docker-compose up
    Go to http://localhost:8080/